import { BlockDefinition } from 'server/block/block';
import { TrapProperties } from 'server/block/trap/properties';

// This block is frozen / WIP
export const TrapDef = BlockDefinition.create('trap', TrapProperties);
