import { BedrockProperties } from 'server/block/bedrock/properties';
import { BlockDefinition } from 'server/block/block';

export const BedrockDef = BlockDefinition.create('bedrock', BedrockProperties);
